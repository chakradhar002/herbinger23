/**
 * The function add takes two parameters, a and b, and returns the sum of a and b
 * @param {number} a - number
 * @param {number} b - number
 * @returns 7
 */
function add(a: number, b: number) {
    return a + b;
  }
  
  console.log(add(2,5))