/* Creating an interface called Rectangle and then creating a constant called rectangle that is of type
Rectangle. */
// Try creating a new interface using it below
interface Rectangle {
    height: number,
    weight: number
  };
  
  const rectangle: Rectangle = {
    height: 5.11,
    weight: 90
  };
  
  console.log(rectangle);