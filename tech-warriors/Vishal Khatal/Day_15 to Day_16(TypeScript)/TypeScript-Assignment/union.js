/**
 * The function printAge() takes in a parameter called code and prints out the string "My Age is "
 * concatenated with the value of code and a period
 * @param code - number
 */
function printAge(code) {
    console.log("My Age is ".concat(code, "."));
}
printAge(23);
printAge('23');
