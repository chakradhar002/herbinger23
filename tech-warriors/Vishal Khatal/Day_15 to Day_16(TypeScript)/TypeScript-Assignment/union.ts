/**
 * The function printAge() takes a parameter of type string or number
 * @param {string | number} code - string | number
 */
function printAge(code: string | number) {
    console.log(`My Age is ${code}.`)
  }
  
  printAge(23);
  printAge('23');